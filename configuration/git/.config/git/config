[user]
	name = Orien Madgwick
	email = _@orien.io
[github]
	user = orien
[init]
	defaultBranch = main
[push]
	default = current
[pull]
	ff = only
[rebase]
	autosquash = true
[alias]
	prune-merged = !git branch --merged | grep -v \"\\*\" | grep -v main | xargs -n 1 git branch -d
[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	features = line-numbers hyperlinks
	hyperlinks-file-link-format = x-mine://open?file={path}&line={line}
	syntax-theme = gruvbox-dark
